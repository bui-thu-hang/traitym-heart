# Heart particles page

Open this page locally after starting a static server from the workspace root:

http://localhost:8000/heart/index.html

To run a quick server (Python 3):

```bash
python3 -m http.server 8000
```

Stop server with Ctrl+C.
